{
  "name": "com.appserviceti.zuluapps",
  "displayName": "App Serviços",
  "version": "1.0.0",
  "description": "A sample Apache Cordova application that responds to the deviceready event.",
  "main": "index.js",
  "scripts": {
    "dev": "npx browser-sync start --server www --files 'www/**/*'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Apache Cordova Team",
  "license": "Apache-2.0",
  "dependencies": {
    "browser-sync": "^3.0.4"
  },
  "devDependencies": {
    "cordova-android": "^14.0.0",
    "cordova-browser": "^7.0.0"
  },
  "cordova": {
    "platforms": [
      "browser",
      "android"
    ]
  }
}